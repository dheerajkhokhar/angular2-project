import { Component } from '@angular/core';
import { SurveyService } from './survey.service'; 

@Component({
    selector: 'survey',
    template: `<h1>d</h1>`,
    styles:[`.surveyList { cursor:pointer }`]
})

export class IndividualSurvey{
  
}